name: Test CI/CD CYNA

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run hello world
        run: echo "Hello CYNA - Workflow GitHub Actions OK"

      - name: Create artifact file
        run: echo "Plan Terraform simulé pour CYNA" > plan.json

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: plan-json
          path: plan.json
          retention-days: 7
